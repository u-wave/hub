{
  "private": true,
  "name": "u-wave-hub",
  "dependencies": {
    "lerna": "^3.0.0-beta.21",
    "standard": "^11.0.0"
  },
  "scripts": {
    "prepare": "lerna bootstrap --no-ci",
    "start": "lerna run --parallel watch",
    "build": "lerna run --stream build",
    "test": "standard"
  },
  "standard": {
    "ignore": [
      "client/out/**",
      "*/dist/*.js"
    ]
  }
}
